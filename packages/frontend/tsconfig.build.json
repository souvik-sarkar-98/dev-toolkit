{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "rootDir": "${configDir}/src"
  },
  "exclude": [
    "${configDir}/**/*.spec.ts",
    "${configDir}/**/*.test.ts"
  ]
}
